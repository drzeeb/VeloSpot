<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Foreground service that keeps recording a ride while the app is in the
         background or closed, with a persistent notification to stop/save it. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <!-- Keep the CPU awake during an active recording so GPS fixes keep arriving
         with the screen off / in Doze (the foreground service alone does not). -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Posting the recording notification (runtime-requested on Android 13+). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Re-arm the "VeloSpot Wrapped" background scheduler after a device reboot,
         since a reboot clears WorkManager's pending one-shot work. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Optional external BLE sensors (speed/cadence/power/heart-rate over the
         standard, vendor-agnostic GATT profiles). Android 12+ splits Bluetooth
         into the runtime SCAN/CONNECT permissions; "neverForLocation" opts out of
         deriving location from BLE scans so the legacy location grant is not
         needed for scanning. The legacy permissions are capped at API 30. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VeloSpot"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:localeConfig="@xml/locale_config"
            android:theme="@style/Theme.VeloSpot">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Open a .gpx file from outside the app (file manager, e-mail
                 attachment, browser download, share sheet, …). GPX has no reliable
                 MIME type, so we match generously: the common GPX/XML MIME types on
                 both content:// and file:// schemes, plus any-MIME (*/*) files whose
                 path ends in ".gpx". The multiple <data> tags mirror how other
                 Android apps register a .gpx handler, catching the many providers
                 that report inconsistent (or missing) types. -->
            <intent-filter android:label="@string/gpx_open_filter_label">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- Match by (reported) MIME type on both schemes. -->
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="application/gpx+xml" />
                <data android:mimeType="application/xml" />
                <data android:mimeType="text/xml" />
                <data android:mimeType="application/octet-stream" />
            </intent-filter>

            <!-- Fallback: match any file (or content) whose path/name ends in
                 ".gpx" regardless of the reported MIME type. host is required for a
                 pathPattern to apply; "*" matches every host. -->
            <intent-filter android:label="@string/gpx_open_filter_label">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="*/*" />
                <data android:host="*" />
                <data android:pathPattern=".*\\.gpx" />
                <data android:pathPattern=".*\\..*\\.gpx" />
                <data android:pathPattern=".*\\..*\\..*\\.gpx" />
            </intent-filter>
        </activity>

        <!-- Keeps a ride recording alive (GPS + stats) while the app is in the
             background or closed, surfacing a stop/save notification. -->
        <service
            android:name=".core.tracking.RideRecordingService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- Quick Settings tile to start/stop a ride recording. -->
        <service
            android:name=".core.tracking.RideRecordingTileService"
            android:exported="true"
            android:icon="@drawable/ic_ride_recording"
            android:label="@string/ride_record_start"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Exposes transient share images (e.g. the ride share card) to other
             apps (WhatsApp, Telegram, Instagram, …) via a content:// URI. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Home-screen widget to start/stop a ride recording. -->
        <receiver
            android:name=".core.tracking.RideRecordingWidget"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/ride_recording_widget_info" />
        </receiver>

        <!-- Re-enqueues the self-rescheduling "VeloSpot Wrapped" worker after a
             reboot or an app update (both clear WorkManager's pending one-shot). -->
        <receiver
            android:name=".feature.wrapped.scheduler.WrappedBootReceiver"
            android:exported="false"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- WorkManager on-demand initialisation: remove the default
             WorkManagerInitializer so the app's Hilt-aware Configuration.Provider
             (BaseApplication) supplies the HiltWorkerFactory instead. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>

</manifest>