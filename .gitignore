*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
/.idea/copilotDiffState.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# ---------------------------------------------------------------------------
# Release signing secrets – NEVER commit these.
# Losing the keystore makes reproducible F-Droid updates impossible (see
# docs/RELEASING.md), so back it up securely OUTSIDE the repository.
# ---------------------------------------------------------------------------
keystore.properties
*.jks
*.keystore
*.p12
*.base64

